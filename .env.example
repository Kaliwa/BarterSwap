# Copy to .env and adjust as needed.
# Connection string used by the API to reach PostgreSQL.
DATABASE_URL=postgres://barterswap:barterswap@localhost:5432/barterswap?sslmode=disable

# HTTP port the API listens on.
PORT=8080
